
import { Candidate, Employer, JobOpening, JobApplication, CandidateStatus, JobStatus, ApplicationStatus, Invoice } from './types';

export const MOCK_CANDIDATES: Candidate[] = [];

export const MOCK_EMPLOYERS: Employer[] = [];

export const MOCK_JOBS: JobOpening[] = [];

export const MOCK_APPLICATIONS: JobApplication[] = [];

export const MOCK_INVOICES: Invoice[] = [];
